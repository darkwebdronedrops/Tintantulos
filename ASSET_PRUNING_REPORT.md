# Asset Pruning Analysis Report

**Generated:** 2026-06-29
**Project:** acanous_floor3_demo
**Analysis scope:** All assets in `assets/` cross-referenced against active (HexFloor) and inactive (old room-based) scripts/scenes

---

## Executive Summary

| Category | Count | Action |
|----------|-------|--------|
| Active hex system references | 310 | **KEEP** |
| Both old and hex references | 10 | **KEEP** |
| Old system ONLY | 272 | **SAFE TO DELETE** |
| Unreferenced by ANY system | 1667 | **REVIEW NEEDED** |
| Orphaned .import files | 0 | None |

**Total base asset files:** 2,249
**Total .import files:** 2,246

---

## Safe to Delete (Old-Only Assets)

These 272 assets are referenced **only** by inactive (non-hex) controllers and old room scenes. Since the game has fully migrated to hex-grid, these are likely safe to remove.

**By category:**
- `assets/sprites/` — 272 files (old room backdrops, portal sprites, boss arena backgrounds, etc.)

**Examples:**
- `bg_crown_cog_hub.png` — old Crown Cog hub background
- `floor1_central.png`, `floor1_east_warren.png`, etc. — old room backdrops
- `portal_east.png`, `portal_north.png`, etc. — old portal sprites
- `boss_arena_bg.png`, `the_door.png` — old boss/Door sprites
- `floor10/bg_approach.png`, `floor10/bg_hoard.png`, etc. — old Floor 10 room backgrounds

**Full list:** See `scripts/unused_assets_old_only.txt` (generate with `grep "res://"` from the old-only set)

---

## Review Needed (Unreferenced Assets)

These 1,667 assets are not referenced by **any** script via `res://` path. However, many are likely used via **string names** (especially audio files).

### Audio Files (161 unreferenced)
**Important:** Audio files are typically referenced by name string in `AudioManager`, not by full `res://` path. The following are flagged as "unreferenced" but may be actively used:

- `audio/ambience/gearworks.wav`
- `audio/music/floor3_gearworks_v2.mp3` (active floor 3 music!)
- `audio/music/floor2_fungal.mp3`, `floor2_fungal_v2_v2.mp3`
- `audio/music/combat_encounter_v1.mp3`, `v2.mp3`
- `audio/sfx/boss_attack.mp3`, `boss_death.mp3`, `boss_entrance.wav`
- `audio/sfx/ambience_calm.mp3`, `ambience_steam.mp3`, `ambience_gearworks.mp3`
- All floor music (floor1 through floor10 variants)
- `main_title.mp3`, `credits_v1.mp3`, `shop_theme_v2.mp3`

**Recommendation:** Check `AudioManager.gd` or `AudioManager` code for string-based audio references before deleting any audio files.

### Sprite Files (1,501 unreferenced)
Many of these may be:
- Old enemy sprites (side-scroller view, now replaced with hex-top-down)
- Old UI elements
- Generated backdrops that were replaced
- Card art that's loaded dynamically via `CardDB` or `CardData`

**Recommendation:** Verify dynamic loading paths before bulk deletion.

### Tile Files (4 unreferenced)
- Check if hex tiles are generated procedurally rather than loaded from files

---

## How to Use This Report

1. **Start with old-only assets** (272 files) — lowest risk
2. **Check AudioManager string references** before touching audio
3. **Check CardDB / CardData dynamic loading** before touching card sprites
4. **Verify hex tile generation** — tiles may be procedural
5. **Keep `.import` files** paired with their base files (Godot needs them)

---

## Old Scene Files Also Candidates

Old room scenes that may be safe to delete:
- `scenes/rooms/Floor1_Central.tscn`, `Floor1_East_Warren.tscn`, etc.
- `scenes/rooms/Floor2_Entry.tscn`, `Floor2_Upper.tscn`, etc.
- All pre-hex room scenes

**Verify:** These scenes are no longer referenced by the active HexFloor controllers before deleting.

---

## Next Steps

1. Generate `old_only_assets.txt` and `unused_assets.txt` for manual review
2. Run a test build after deleting old-only assets to confirm no regressions
3. Check AudioManager string references for audio files
4. Check CardDB for dynamic sprite loading paths

---

*Report generated by automated asset cross-reference analysis*
